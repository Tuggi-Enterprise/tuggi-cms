# Skip optional dependencies to avoid platform issues
optional=false

# Use npm's built-in SWC instead of platform-specific ones
engine-strict=false

# Force npm to ignore platform-specific packages
target_platform=linux
target_arch=x64
