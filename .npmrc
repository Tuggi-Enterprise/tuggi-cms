# Next.js will automatically install the correct SWC package for the build platform
# Don't fail on optional dependencies (platform-specific SWC packages)
optional=true
engine-strict=false
# Allow npm to install platform-specific optional dependencies
legacy-peer-deps=false
