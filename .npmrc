# Next.js will automatically install the correct SWC package for the build platform
# During build, npm will skip optional dependencies that don't match the platform
# This prevents errors when building on different platforms (e.g., Vercel Linux vs local macOS)
legacy-peer-deps=false
engine-strict=false
