# Next.js will automatically install the correct SWC package for the build platform
# npm will skip optional dependencies that don't match the current platform automatically
legacy-peer-deps=false
engine-strict=false
