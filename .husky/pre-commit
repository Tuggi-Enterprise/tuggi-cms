echo "🔍 Verificando erros de TypeScript e ESLint..."

npm run check-all
if [ $? -ne 0 ]; then
  echo "❌ check-all falhou (TypeScript ou ESLint). Commit cancelado."
  exit 1
fi

echo "✅ Todas as verificações passaram!"
