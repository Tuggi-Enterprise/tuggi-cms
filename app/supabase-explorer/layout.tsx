import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Supabase Explorer - Tuggi CMS',
  description: 'Database explorer for Supabase cloud instance',
}

export default function SupabaseExplorerLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}










