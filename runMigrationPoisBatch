#!/usr/bin/env bash
set -euo pipefail

set -a
source .env
set +a

npx tsx scripts/migrate-pois-batch.ts --country "Spain" --batch-size 50000 --mode "enrichment_migration_triggers" --auto-generate-audio false --auto-approve true